# Railway Environment Variables Example
# Copy these to your Railway project settings

# Required - Set to 'prod' for production deployment
SPRING_PROFILES_ACTIVE=prod

# Required - Generate a secure random string (min 32 characters)
# Use: openssl rand -base64 32
# Or PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
JWT_SECRET=your-secure-jwt-secret-here-minimum-32-characters

# Auto-set by Railway when you add MySQL database
# Format: mysql://user:password@host:port/database
# Railway converts this to JDBC format automatically
DATABASE_URL=mysql://root:password@containers-us-west-123.railway.app:3306/railway

# Auto-set by Railway (usually 8080)
PORT=8080
